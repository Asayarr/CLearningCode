# ============================================
#  C 学习仓库 .gitignore
#  原则：只提交源码(.c/.h)，忽略"编译产出的东西"
#  注意：.gitignore 不支持行尾注释，注释必须单独占一行
# ============================================

# ---------- 编译中间文件 / 常见产物 ----------
*.o
*.obj
*.out
a.out
*.exe
*.dll
*.so
*.dSYM/
*.gch

# ---------- 本仓库里 gcc 生成的可执行文件 ----------
# 习惯是 gcc exN.c -o exN：可执行文件和源文件同名、且没有扩展名。
# 先忽略所有 "ex+数字" 开头的，再用 ! 把 .c 源文件捞回来。
ex[0-9]*
!ex[0-9]*.c
# 其他手动命名的可执行文件：
hello
int_demo
proj1/calc_app

# ---------- 编辑器 / 操作系统 ----------
# 想保留 VS Code 配置就删掉下面这行
.vscode/
*.swp
*~
.DS_Store
