# C 语言学习笔记

> 基于 C# 基础快速上手 C —— 整理自第 1~6 课
> 配套练习文件：`hello.c` `int_demo.c` `ex1.c` ~ `ex9.c` `myutils.*` `proj1/*`

---

## 目录

0. [环境与工具链](#0-环境与工具链)
1. [程序结构](#1-程序结构)
2. [基本数据类型](#2-基本数据类型)
3. [输入输出 printf / scanf](#3-输入输出-printf--scanf)
4. [运算符与类型转换](#4-运算符与类型转换)
5. [控制流](#5-控制流)
6. [函数](#6-函数)
7. [指针](#7-指针核心)
8. [数组](#8-数组)
9. [字符串](#9-字符串)
10. [多文件编译与头文件](#10-多文件编译与头文件)
11. [常用库函数速查](#11-常用库函数速查)
12. [常见坑总结](#12-常见坑总结)
13. [C# → C 对照表](#13-c--c-对照表)

---

## 0. 环境与工具链

### 工具是分开的（不像 Visual Studio 全包）

| 工具 | 作用 | C# 对应 |
|------|------|---------|
| 编辑器（VS Code / nano / micro） | 写代码 | 编辑窗口 |
| **gcc** | 编译 | `dotnet build` / `csc` |
| ld | 链接（gcc 自动调用） | — |
| gdb | 调试 | 调试器 |
| make / cmake | 构建 | MSBuild |

### 编译运行三步

```bash
cd ~/c-learning
gcc 文件.c -o 输出名 -Wall -Wextra
./输出名
```

- `-Wall -Wextra`：打开警告。**C 编译器很宽容，警告是重要的 bug 信号，务必看**
- 不加 `-o` 默认生成 `a.out`
- 编译成功=**无输出**（没消息就是好消息）

### 多文件编译

```bash
gcc main.c myutils.c -o app -Wall -Wextra        # 一步到位
# 或分步（更接近真实项目）
gcc -c main.c -o main.o -Wall -Wextra            # 只编译，生成 .o
gcc -c myutils.c -o myutils.o -Wall -Wextra
gcc main.o myutils.o -o app                      # 链接
```

---

## 1. 程序结构

```c
#include <stdio.h>          // 预处理：把头文件内容"粘贴"到这

int add(int a, int b) {     // 自定义函数（C 没有类，函数都在顶层）
    return a + b;
}

int main(void) {            // 入口，必须有且只有一个
    printf("%d\n", add(1, 2));
    return 0;               // 0 = 成功；非 0 = 失败
}
```

- **C 没有类、没有命名空间、没有方法**，只有"一堆函数 + 一个 main"
- `#include` 是**预处理指令**：把目标文件**原封不动复制粘贴**到这一行
  - `#include <stdio.h>`：尖括号 = 系统头文件
  - `#include "myutils.h"`：双引号 = 自己的头文件（先找当前目录）
- `printf` 靠 `<stdio.h>` 里的**声明**；**实现**在标准库里，链接时接上
- `main` 返回值是**退出码**，shell 用 `echo $?` 读取（0 成功，非 0 失败）
- 合法写法只有 `int main(void)` 或 `int main(int argc, char *argv[])`
  - 别写 `void main`（非标准，gcc 会警告 `-Wmain`）
- C99 起不写 `return` 会隐式返回 0，但**建议显式写**

---

## 2. 基本数据类型

| 类型 | C# 对应 | 本机大小 | printf | scanf |
|------|---------|---------|--------|-------|
| `char` | `byte`/`sbyte` | 1 字节 | `%c` | `%c` |
| `short` | `short` | 2 字节 | `%d` | `%hd` |
| `int` | `int` | 4 字节 | `%d` | `%d` |
| `unsigned int` | `uint` | 4 字节 | `%u` | `%u` |
| `long` | `long` | **8 字节**（Linux）/ 4（Windows）| `%ld` | `%ld` |
| `long long` | `long` | 8 字节 | `%lld` | `%lld` |
| `float` | `float` | 4 字节 | `%f` | `%f` |
| `double` | `double` | 8 字节 | `%f` | `%lf` |
| `size_t` | — | 8 字节 | `%zu` | — |
| 指针 `void*` | — | 8 字节 | `%p` | — |

要点：
- **`int` 的大小不保证**（标准只说 ≥16 位）。`long` 在 Linux 是 8、Windows 是 4——这就是坑
- 用 `sizeof(类型/变量)` 查询（是**运算符**不是函数），结果类型是 `size_t`，打印用 `%zu`
- **没有 `bool`**：用 `int`，0 假、非 0 真（C99 有 `<stdbool.h>` 但少用）
- **`char` 是 1 字节整数**：`char c = 'A';` 等价于 `char c = 65;`
- **`char` 用单引号 `'A'`；双引号 `"A"` 是字符串，两者不同**
- 默认有符号，等价于 `signed int`；`unsigned` 前缀表示非负
- ⚠️ 局部变量**不初始化就是垃圾值**（不是 C# 的默认 0），**永远要初始化**
- ⚠️ **整数溢出**：`int b = 2147483647; b = b + 1;` → 变成 `-2147483648`，**不报错**

---

## 3. 输入输出 printf / scanf

### 格式串是"模板"

`printf` 第一个参数是**输出模板**：普通字符原样打印，`%` 占位符被替换。

```c
printf("成绩：%d 分\n", 95);     // 输出：成绩：95 分（并换行）
```

### 转义字符

| 转义 | 含义 |
|------|------|
| `\n` | 换行 |
| `\t` | 制表符 |
| `\\` | 反斜杠 |
| `\0` | 空字符 NUL（ASCII 0）|

### 常用格式符

```c
printf("%d\n", 42);           // 整数
printf("%.2f\n", 3.14159);    // 浮点，保留2位 → 3.14
printf("%c\n", 'A');          // 字符
printf("%s\n", "hello");      // 字符串
printf("%5d\n", 42);          // 宽度5右对齐 → "   42"
printf("%p\n", (void*)p);     // 地址
printf("%zu\n", sizeof(int)); // size_t
```

⚠️ **`printf` 第一个参数必须是格式串，不能直接传变量**：
```c
printf(a);          // ❌ 错！
printf("%d\n", a);  // ✅ 对
```
占位符的**个数和类型必须和后面变量匹配**，否则输出乱码或崩溃。

### scanf 读输入

```c
int age;
printf("请输入年龄：");
scanf("%d", &age);           // ← 注意 &！
```

⚠️ **关键规则**：
1. **`scanf` 的格式串只能放格式符，不能写提示文字！**
   ```c
   scanf("请输入名字%s", name);  // ❌ 会去输入里匹配这些中文，失败
   printf("请输入名字：");       // ✅ 提示归 printf
   scanf("%s", name);
   ```
2. **读普通变量要加 `&`（取地址符）**，告诉 scanf 变量住哪
   - 读数组/字符串**不用** `&`（因为它本身就是地址）
3. 会跳过空格/换行；`%s` **遇到空格就停**
4. `scanf` **返回成功读到的个数**，可用来判断输入是否合法：
   ```c
   if (scanf("%d", &age) != 1) { /* 输入非法 */ }
   ```

---

## 4. 运算符与类型转换

和 C# 基本一致：`+ - * / % ++ --`、`== != < > <= >=`、`&& || !`、`+= -=` 等。

注意：
- **整数除法会截断**：`10 / 3` = `3`（不是 3.33）
- **整数取模**：`10 % 3` = `1`
- **除以零**：整数 `a / 0` 直接崩溃（不是异常！）
- 隐式转换：小范围→大范围安全（`int`→`double`）
- 显式强转：`(int)3.9` = `3`（直接砍小数，**不四舍五入**）
- ⚠️ 有符号和无符号混用会有诡异行为，尽量别混

---

## 5. 控制流

和 C# 几乎一样，可直接迁移：

```c
if (x > 0) { ... } else if (x == 0) { ... } else { ... }

for (int i = 0; i < n; i++) { ... }

while (cond) { ... }
do { ... } while (cond);

switch (x) {
    case 1: ...; break;     // ⚠️ C 的 switch 不加 break 会"贯穿"到下一个 case
    default: ...; break;
}
```

> ⚠️ `switch` 的 **case 必须显式 `break`**，否则会继续执行下一个 case（C# 里不允许贯穿，C 里却允许，是常见 bug）。

---

## 6. 函数

```c
// 定义：返回类型 函数名(参数)
int add(int a, int b) {
    return a + b;
}

// 声明（原型）：只写签名，分号结尾。用于"定义在后面"时提前告诉编译器
int add(int a, int b);

void print_hello(void) {       // void = 无返回值 / 无参数
    printf("hi\n");
}
```

### 传值 vs 传地址（核心）

**C 的参数一律"传值"（复制一份）**，函数里改的是副本：

```c
void square_wrong(int n) { n = n * n; }   // 改的是副本
int a = 6;
square_wrong(a);        // a 还是 6！
```

想改调用者的变量，**必须传指针**（C 没有 `ref`）：

```c
void square(int *n) { *n = (*n) * (*n); }
square(&a);             // a 变成 36
```

三种"把结果送出函数"的方式：
| 方式 | 写法 | 场景 |
|------|------|------|
| 返回值 | `a = f(a);` | 只输出一个结果，最清晰 |
| 指针参数 | `f(&a);` | 要改调用者变量 / 输出多个结果 |
| 指针参数（数组） | `f(arr, n);` | 传数组、避免复制 |

---

## 7. 指针（核心）

### 直觉

每个变量都住在内存某个位置，有**地址**。
- `&变量`：取地址
- 指针：**存地址的变量**
- `*指针`：解引用，顺着地址找到那个变量

### 声明与使用

```c
int a = 42;
int *p = &a;        // 声明"指向 int 的指针"，指向 a

printf("%d\n", *p); // 解引用 → 42
*p = 100;           // 改 p 指向的内存 → a 也变 100
```

⚠️ **`*` 有两个身份**：
- 类型后面跟名字 = **声明指针**：`int *p;`
- 单独用在变量前 = **解引用**：`*p = 100;`

### 两种语义（极其重要）

```c
int *p = &x;   // 别名：p 指向 x，改一个另一个跟着变
int b = a;     // 复制：b 是 a 的副本，各改各的互不影响
```
> C 里 `=` 默认是**复制值**，想要"引用同一份"就得用指针。

### 指针占多大
`sizeof(int*)` = 8 字节（64 位系统），和它指向什么类型无关。

---

## 8. 数组

| | C# | C |
|---|---|---|
| 声明 | `int[] a = new int[5];` | `int a[5];` |
| 取长度 | `a.Length` | ❌ 没有 |
| 越界 | 抛异常 | ❌ 不检查，未定义行为 |

```c
int a[5];                      // 5 个 int，未初始化（垃圾值）
int b[5] = {10,20,30,40,50};  // 全部初始化
int c[]  = {1,2,3};           // 自动大小 = 3
int d[5] = {1,2};             // 前两个 1,2，其余补 0

a[0] = 100;                    // 下标从 0 开始
```

### 核心：数组名 = 首元素地址

```c
int arr[5] = {10,20,30,40,50};
arr        // 就是 &arr[0]，一个地址
arr[i]     // 等价于 *(arr + i) 等价于 *(p + i)
```

### 指针运算

**指针 +1 移动的是"一个元素的大小"**：

| 类型 | `p+1` 实际移动 |
|------|---------------|
| `int*` | +4 字节 |
| `char*` | +1 字节 |
| `double*` | +8 字节 |

### 数组传参

```c
void print_arr(int *arr, int n) {   // 等价写法：int arr[]
    for (int i = 0; i < n; i++) printf("%d ", arr[i]);
}
int a[] = {1,2,3};
print_arr(a, 3);                    // 传数组名（已是地址，不用 &）
```

⚠️ **必须额外传长度 `n`**：数组传进函数后**退化成指针**，`sizeof(arr)` 得到的是指针大小（8），不是数组大小。
> 求长度套路：`sizeof(a)/sizeof(a[0])`（只能在定义数组的作用域里用）

> `arr` vs `&arr`：地址值相同，但类型不同。`arr` 是 `int*`（+1 移动 4 字节），`&arr` 是 `int(*)[5]`（+1 移动整个数组）。传参用 `arr`。

---

## 9. 字符串

### 本质：`char` 数组 + 结尾的 `\0`

```c
char s[] = "hello";     // 实际占 6 字节：h e l l o \0
sizeof(s)               // 6
strlen(s)               // 5（不含 \0）
```

- `'\0'`（NUL，ASCII 0）是**结束标记**
- `printf("%s")` 从头打印，遇到 `\0` 停
- 别混淆：`'\0'` = ASCII 0；`'0'` = ASCII 48

### 三种声明

```c
char s1[] = "hello";      // ① 数组，可改，大小自动 6
char s2[20] = "hello";    // ② 数组，预留 20 字节（安全）
char *s3 = "hello";       // ③ 指针 → 指向只读字面量【不能改！】
// s3[0] = 'H';  ❌ 崩溃
```

### 关键陷阱：不能用 `=` 和 `==`

```c
char a[] = "hi", b[10];
b = a;                    // ❌ 编译错误（数组不能整体赋值）
if (a == b) { }           // ❌ 合法但错！比较的是地址，不是内容

strcpy(b, a);             // ✅ 复制内容
if (strcmp(a, b) == 0)    // ✅ 比较内容（==0 表示相等）
```

### 读取字符串

```c
char name[20];
scanf("%s", name);        // 注意：不加 &（name 是数组=地址）
// ⚠️ 遇空格停、不检查长度（会溢出）

fgets(name, sizeof(name), stdin);   // 更安全：读整行且限长
```

### ⚠️ 缓冲区溢出
`strcpy` 等函数**不检查目标大小**，写爆就是经典安全漏洞。

---

## 10. 多文件编译与头文件

### 约定：`.h` 放声明，`.c` 放实现

**`math_utils.h`**
```c
#ifndef MATH_UTILS_H      // 头文件保护：防止被重复 include
#define MATH_UTILS_H

int add(int a, int b);    // 只写声明
int mul(int a, int b);

#endif
```

**`math_utils.c`**
```c
#include "math_utils.h"   // 包含自己的头文件，保证声明/实现一致

int add(int a, int b) { return a + b; }
int mul(int a, int b) { return a * b; }
```

**`main.c`**
```c
#include <stdio.h>
#include "math_utils.h"

int main(void) {
    printf("%d\n", add(3, 5));
    return 0;
}
```

**编译**：`gcc main.c math_utils.c -o app`

### 编译 vs 链接（重要区别）

| 问题 | 阶段 | 报错 |
|------|------|------|
| 漏了 `.h` | **编译**期 | `fatal error: xxx.h: No such file or directory` |
| 漏了 `.c` | **链接**期 | `undefined reference to 'add'`（找不到函数**实现**）|

> `#include` 让编译器**认识**函数（有声明即可编译过）；链接器负责把**实现**接起来。两者是不同阶段。

### 头文件保护
```c
#ifndef XXX_H      // 如果没定义过
#define XXX_H      // 就定义它
// ... 内容 ...
#endif
```
或用 `#pragma once`（更简单，广泛支持）。

---

## 11. 常用库函数速查

### `<stdio.h>`
| 函数 | 作用 |
|------|------|
| `printf(fmt, ...)` | 格式化输出 |
| `scanf(fmt, ...)` | 格式化输入 |
| `fgets(buf, n, stdin)` | 安全读一行 |
| `fopen` / `fclose` | 打开/关闭文件（第 9 课）|
| `fprintf` / `fscanf` | 文件读写（第 9 课）|

### `<string.h>`
| 函数 | 作用 | C# 对应 |
|------|------|---------|
| `strlen(s)` | 长度（不含 `\0`）| `s.Length` |
| `strcpy(dst, src)` | 复制 | `b = a` |
| `strcmp(a, b)` | 比较，相等返回 0 | `a.CompareTo(b)` |
| `strcat(dst, src)` | 拼接 | `a + b` |
| `strchr(s, c)` | 找字符 | `IndexOf` |

### `<stdlib.h>`
| 函数 | 作用 |
|------|------|
| `malloc` / `free` | 动态内存（第 8 课）|
| `atoi` / `atof` | 字符串转数字 |
| `abs` | 绝对值 |
| `rand` / `srand` | 随机数 |

### `<math.h>`
`sqrt`、`pow`、`fabs` 等；编译时可能需加 `-lm`。

---

## 12. 常见坑总结

1. `printf` 第一个参数必须是格式串，不能直接传变量
2. `scanf` 格式串**不能写提示文字**；读普通变量**要加 `&`**
3. 漏 `&` → 程序崩溃/诡异行为，编译器不一定报错
4. 数组**越界不报错**（未定义行为）
5. 数组不能 `=` 赋值、不能用 `==` 比较（要用 `strcpy`/`strcmp`）
6. `sizeof` / `strlen` 返回 `size_t`，打印用 `%zu`
7. 局部变量**不初始化 = 垃圾值**
8. **整数溢出**不报错，会绕回
9. **除以零**直接崩溃（不是异常）
10. `switch` 不写 `break` 会"贯穿"
11. 整数除法会**截断**小数
12. 传数组给函数会**退化成指针**，`sizeof` 失真，必须另传长度
13. `char *s = "字面量"` 是**只读的**，不能改
14. `.gitignore` 不支持行尾注释；已跟踪文件要 `git rm --cached`

---

## 13. C# → C 对照表

| C# | C |
|----|----|
| `using System;` | `#include <stdio.h>`（机制不同：粘贴）|
| `class` / 方法 | 只有函数（顶层）|
| `static void Main()` | `int main(void)` |
| `Console.WriteLine` | `printf(...\n)` |
| `Console.ReadLine` + `Parse` | `scanf` / `fgets` + `atoi` |
| `string`（不可变对象）| `char[]` + `\0`（可变）|
| `bool` | 用 `int`（0 假，非 0 真）|
| `int`（永远 4 字节）| `int`（大小不保证）|
| `List<int>` | `int*` + `malloc` + 手动记长度 |
| `ref` 参数 | 传指针 `&` / `*` |
| 委托 | 函数指针 |
| 泛型 | 无（`void*` / 宏）|
| 异常 | 返回错误码 / `errno` |
| GC 自动回收 | `malloc` / `free` 手动管理 |
| 数组带边界检查 | 无边界检查 |

---

## 尚未学习（后续课程）

- [ ] **结构体 `struct`**（第 7 课）
- [ ] **动态内存 `malloc` / `free`**（第 8 课）
- [ ] **文件读写**（第 9 课）
- [ ] **指针进阶**：二级指针、函数指针（第 10 课）
- [ ] 综合项目：通讯录管理系统
